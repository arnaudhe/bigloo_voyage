<IfModule mod_rewrite.c>    
    Options -MultiViews

    RewriteEngine On
    Options FollowSymLinks
    #RewriteBase /silex-bootstrap
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>
